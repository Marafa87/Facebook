"Définition du schema mongo Db"
db.createCollection("Facebook", {
   validator: {
      $jsonSchema: {
        bsonType: "object",
        required: ["Person", "Page", " Group", "Comment", "Post",  "Messages", "Stockage"],
        properties: {
        Person: {
                bsonType: "object",
                required: [ "Name", "Surname", "Email", "Phonenumber", "Password" ],
            properties: {
                Name: {
                    bsonType: "string",
                    description: "must be an alphanumeric"
                },
                Surname: {
                    bsonType: "string",
                    description: "must be an alphanumeric"
                },

                Email: {
                    bsonType: "string",
                    description: "must be an alphanumeric"
                },

                Phonenumber: {
                    bsonType: "string",
                    description: "must be a Number"
                },

                Password: {
                    bsonType: "number",
                    description: "must be an alphanumeric"
                }		
            
            }
            }

        }
    }
    }
    })